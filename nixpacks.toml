[phases.setup]
nixPkgs = ['python312', 'python312Packages.pip', 'gcc']

[phases.install]
cmds = ['python3 -m pip install --user -r requirements.txt']

[phases.build]
cmds = []

[start]
cmd = 'python3 -m gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120'
